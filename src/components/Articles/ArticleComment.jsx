// import React, { useState } from 'react';
// import PropTypes from 'prop-types';

// const UserAvatar = ({ nickName, role, avatar }) => {
//   return <div>{`${nickName} ${role} ${avatar}`}</div>;
// };
const ArticleComment = () => {
  // const ArticleComment = ({ comment }) => {
  // const { commentText: text, commentDateTime: date, author, nested } = comment;
  // const nestedComments = nested.map(item => <ArticleComment key={item.id} comment={item} />);
  // return (
  //   <div>
  //     <UserAvatar {...author} />
  //     {text}
  //     {date}
  //     {nestedComments}
  //   </div>
  // );
};

// ArticleComment.defaulProps = {};
//
// ArticleComment.propTypes = {
//
// };

export default ArticleComment;
